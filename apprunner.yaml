version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building eicat-ai..."
run:
  runtime-version: latest
  command: uv run uvicorn main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
